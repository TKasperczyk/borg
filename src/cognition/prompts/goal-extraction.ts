import { BORG_HOST_CAPABILITY_BOUNDARY_PROMPT } from "./host-capabilities.js";
import { SELF_REFERENTIAL_MEMORY_VOICE_GUIDANCE } from "../../util/self-memory-voice.js";

export const GOAL_PROMOTION_SYSTEM_PROMPT = [
  "Classify goal-like candidates from the current user turn by memory kind.",
  "Only durable_borg_goal becomes active goal self-memory; other labels are rejected from goal storage but explain why.",
  "Set durable_goal_batch=explicit_multiple only when the current turn explicitly asks Borg to track multiple separate ongoing responsibilities; otherwise use single.",
  "Judge semantic intent across languages. Do not rely on wording, punctuation, capitalization, or phrase shapes.",
  "The supplied active_goals list is the complete global set of active goals across audiences and owners.",
  "Durable goals are about Borg's durable conversation/memory responsibility, not participants' personal life goals. A user saying \"my goal is to...\" is usually participant-side context, a preference, or one_off planning context, not durable_borg_goal.",
  "Before emitting durable_borg_goal, compare the candidate against every supplied active goal. An active goal covers the candidate when it represents the same underlying Borg responsibility or completion outcome, even when the responsibility is rephrased, translated, renewed later, or surfaced through another audience or owner; the current turn need not refer to the prior goal. Audience, owner, session, wording, language, and elapsed time do not by themselves make a second goal. When an active goal already covers the candidate, classify as already_represented and set duplicate_of_goal_id.",
  "Use target_at only for a real goal deadline. Use the supplied temporal cue as context, not as an automatic trigger. Resolve relative or year-less dates against the supplied current_time and write the resolved calendar date, including its year, as an ISO-8601 date.",
  "Borg-owned durable goals must stay inside the host capability boundary below.",
  `${SELF_REFERENTIAL_MEMORY_VOICE_GUIDANCE} Apply this to durable_borg_goal descriptions. For goals or tasks belonging to other participants, name those participants rather than using deictic placeholders.`,
  BORG_HOST_CAPABILITY_BOUNDARY_PROMPT,
  "",
  "Classifications:",
  "- durable_borg_goal: Borg owns a prospective durable conversation/memory responsibility with a meaningful future completion state that later evidence can establish: a resolvable question, a deliverable to produce or track to completion, or a decision to reach. A description of the current conversation, topic, exchange, or what Borg is presently tracking is context, not a goal. If you cannot state a meaningful terminal condition, route the candidate to one_off or none; do not invent a terminal condition merely to qualify the candidate.",
  "- one_off: finite event, task, reply, or fact; no ongoing tracking.",
  '- not_borg_responsibility: responsibility belongs to a participant or external system, including participant-owned personal goals such as "my goal is to deploy", "friend will respond", or "a participant will pull flight numbers".',
  "- impossible_for_borg_without_capability: the candidate asks Borg to do work Borg cannot do without host capabilities, such as monitoring p95, sending something later, scheduled document edits, physical attendance, payments, proactive notifications, external tool execution, or production/dashboard monitoring.",
  "- already_represented: a supplied active goal already covers the same underlying Borg responsibility or completion outcome, regardless of rephrasing, translation, elapsed time, audience, or owner; set duplicate_of_goal_id.",
  "- none: not memory-worthy at all.",
  "",
  "For every durable_borg_goal, set terminal_condition to the meaningful future completion condition in the user's own language that later evidence can establish. Use null for non-durable classifications. Do not use a paraphrase of the current conversation state as a completion condition.",
  "Prefer one durable_borg_goal at most. When uncertain, emit no promotions. Return only the required tool call.",
  "",
  "Examples:",
  "- Coding: carry refactor decisions until the migration plan is settled -> durable_borg_goal; read this file -> one_off; user will deploy -> not_borg_responsibility; Borg will monitor p95 -> impossible_for_borg_without_capability.",
  "- Relationships: help keep job-search context until the application plan is decided -> durable_borg_goal; my goal is to get a job -> not_borg_responsibility; send one message -> one_off; friend will respond -> not_borg_responsibility.",
  "- Planning: track the plan across sessions until the itinerary is finalized -> durable_borg_goal; execute tonight's agenda -> one_off; a participant will pull flight numbers -> not_borg_responsibility; Borg will attend in person or make payments -> impossible_for_borg_without_capability.",
  "- Carry deployment-cleanup decisions to sign-off and job-search support to an agreed application plan -> explicit_multiple only if both are separate Borg memory/conversation responsibilities, not external monitoring.",
].join("\n");
