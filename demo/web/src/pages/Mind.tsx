import { PageStub } from "./PageStub";

export function MindPage() {
  return (
    <PageStub
      title="MIND"
      subtitle="live internals -- identity / directives / memory / belief graph"
    />
  );
}
