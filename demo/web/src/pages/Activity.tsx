import { PageStub } from "./PageStub";

export function ActivityPage() {
  return (
    <PageStub
      title="ACTIVITY"
      subtitle="what the entity did -- every turn, wake, and dream, without asking it"
    />
  );
}
