import { PageStub } from "./PageStub";

export function SettingsPage() {
  return (
    <PageStub
      title="SETTINGS"
      subtitle="prompt blocks · runtime · scheduler · entities · danger"
    />
  );
}
